# Exemple de fichier d'environnement pour le projet
# Copiez ce fichier en `.env` ou `.env.local` et ajustez les valeurs.

TZ=UTC
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info
# Générez une clé aléatoire (par ex. `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`)
APP_KEY=ChangeMeReplaceWithARealRandomKey
NODE_ENV=development
SESSION_DRIVER=cookie

# Base de données PostgreSQL
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=app