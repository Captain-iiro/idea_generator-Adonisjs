version: '3.8'

services:
  app:
    container_name: idea_generator_app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '80:3333'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
